# Configuration

Store non-secret, version-controlled settings here, such as observation windows,
random seeds, catalog eligibility rules, score weights, and portfolio constraints.

Credentials belong in environment variables or ignored local secret files, never
in this directory.
