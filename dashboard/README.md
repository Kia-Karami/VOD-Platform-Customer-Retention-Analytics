# Dashboard

This directory will contain the interactive dashboard entry point, reusable
components, and dashboard-specific assets.

The dashboard must consume curated outputs from `data/processed/`; it should not
duplicate analytical transformations from `sql/` or `src/`.
