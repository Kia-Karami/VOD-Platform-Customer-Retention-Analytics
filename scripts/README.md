# Scripts

This directory will hold thin command-line entry points for data acquisition,
synthetic-data generation, pipeline execution, validation, and report exports.

Business logic belongs in the Python package or SQL layers so it remains testable
and reusable.
