# Notebooks

Notebooks are narrative entry points for exploration and communication. Number
them in execution order (for example, `01_data_validation.ipynb`) and keep reusable
logic in `src/vod_retention/` or `sql/`.

Before committing a notebook, restart the kernel, run all cells, verify outputs,
and remove sensitive or unnecessarily large embedded data.
