# Tests

Tests will cover Python units, schema expectations, data-quality rules, SQL output
invariants, scoring behavior, portfolio constraints, and deterministic generation.

Small fixtures belong in `data/sample/`; production-sized data must not be required
for the default test suite.
